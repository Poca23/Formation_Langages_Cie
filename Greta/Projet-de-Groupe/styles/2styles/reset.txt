/* Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  font-size: 16px; /* Base typographique */
  scroll-behavior: smooth;
}

body {
  font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, sans-serif; /* Typographie */
  font-size: 1rem; /* Taille par défaut */
  color: var(--text-color);
  background-color: var(--background-color);
  line-height: 1.6; /* Espacement pour la lisibilité */
  transition: all 0.3s ease;
}

h1, h2, h3, h4, h5, h6 {
  margin-bottom: 0.5rem;
  font-weight: 600;
  line-height: 1.25;
}

a {
  color: var(--primary-color);
  text-decoration: none;
  transition: color 0.2s;
}

a:hover {
  color: var(--primary-hover);
}

button {
  font-family: inherit;
  font-size: inherit;
  cursor: pointer;
}

button:disabled {
  cursor: not-allowed;
  opacity: 0.6;
}

.container {
  display: flex;
  flex-direction: column;
  min-height: 100vh; /* Ajuste automatiquement pour éviter le débordement */
}
