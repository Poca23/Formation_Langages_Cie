:root {
--primary-color: #7c3aed;
--primary-hover: #6d28d9;
--secondary-color: #9333ea;
--background-color: #f3f4f6;
--background-btn: #e7e7e7;
--sidebar-color: #1f2937;
--sidebar-hover: #374151;
--text-color: #111827;
--text-light: #9ca3af;
--border-color: #e5e7eb;
--message-bg-user: #eef2ff;
--message-bg-bot: #ffffff;
--dropdown-bg: #ffffff;
--dropdown-hover: #f3f4f6;
--shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
--shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
--radius-sm: 0.375rem;
--radius-md: 0.5rem;
--radius-lg: 0.75rem;
}

/* Dark Mode */
body.dark-mode {
--background-color: #111827;
--sidebar-color: #1f2937;
--text-color: #f9fafb;
--text-light: #d1d5db;
--border-color: #374151;
--message-bg-user: #374151;
--message-bg-bot: #1f2937;
--dropdown-bg: #1f2937;
--dropdown-hover: #374151;
}

* {
margin: 0;
padding: 0;
box-sizing: border-box;
}

body {
font-family: "Inter", system-ui, -apple-system, sans-serif;
background-color: var(--background-color);
color: var(--text-color);
line-height: 1.5;
transition: background-color 0.3s ease;
}

.container {
display: flex;
height: 100vh;
overflow: hidden;
}

/* Sidebar Styles mis à jour */
.sidebar {
width: 280px;
background-color: var(--sidebar-color);
color: white;
padding: 1rem;
display: flex;
flex-direction: column;
transition: transform 0.3s ease;
z-index: 50;
height: 100%;
}

.sidebar-header {
padding: 0.5rem 0;
}

.new-chat-btn {
width: 100%;
padding: 0.75rem;
background-color: var(--primary-color);
border: none;
border-radius: var(--radius-md);
color: white;
cursor: pointer;
display: flex;
align-items: center;
justify-content: center;
gap: 0.5rem;
transition: background-color 0.2s;
font-size: 0.875rem;
}

.new-chat-btn:hover {
background-color: var(--primary-hover);
}

.section-title {
font-size: 0.75rem;
text-transform: uppercase;
color: var(--text-light);
padding: 1rem 0.75rem 0.5rem;
letter-spacing: 0.05em;
}

.conversations-list {
flex: 1;
display: flex;
flex-direction: column;
gap: 0.25rem;
overflow-y: auto;
}

.conversation-item {
padding: 0.75rem;
border-radius: var(--radius-md);
cursor: pointer;
display: flex;
align-items: center;
gap: 0.75rem;
color: var(--text-light);
position: relative;
transition: all 0.2s ease;
}

.conversation-item svg:first-child {
width: 20px;
height: 20px;
opacity: 0.7;
}

.conversation-item .menu-icon {
margin-left: auto;
opacity: 0;
transition: opacity 0.2s;
}

.conversation-item:hover {
background-color: var(--sidebar-hover);
color: white;
}

.conversation-item:hover .menu-icon {
opacity: 0.7;
}

.conversation-item.active {
background-color: var(--primary-color);
color: white;
}

.conversation-item.active svg {
opacity: 1;
}

.logout-btn {
margin-top: auto;
padding: 0.75rem;
background: transparent;
border: none;
color: var(--text-light);
cursor: pointer;
display: flex;
align-items: center;
gap: 0.5rem;
width: 10%;
border-radius: var(--radius-md);
transition: all 0.2s;
}

.logout-btn:hover {
background-color: var(--sidebar-hover);
color: white;
}

.logout-btn svg {
opacity: 0.7;
}

.logout-btn:hover svg {
opacity: 1;
}

/* Le reste du CSS reste inchangé... */
/* Main Content Styles */
.main-content {
flex: 1;
display: flex;
flex-direction: column;
background-color: var(--background-color);
position: relative;
overflow: hidden;
}

.chat-header {
padding: 1rem;
border-bottom: 1px solid var(--border-color);
background-color: var(--background-color);
display: flex;
align-items: center;
justify-content: space-between;
position: relative;
z-index: 40;
}

/* Header Logo Styles */
.header-logo {
position: absolute;
left: 50%;
transform: translateX(-50%);
display: flex;
align-items: center;
}

.logo {
width: 60px;
height: 60px;
object-fit: contain;
}

/* Header Controls */
.header-left,
.header-right {
display: flex;
align-items: center;
gap: 1rem;
z-index: 1;
}

/* Modification du bouton toggle-sidebar */
.toggle-sidebar {
position: fixed; /* Position fixe pour rester visible */
left: 280px; /* Largeur de la sidebar */
top: 1rem; /* Alignement vertical */
z-index: 60; /* Z-index supérieur à la sidebar */
background-color: var(--background-color); /* Fond pour la visibilité */
border: 1px solid var(--border-color);
transform: rotate(180deg);
transition: left 0.3s ease, transform 0.3s ease;
}

.sidebar.active + .main-content .toggle-sidebar {
left: 290px; /* Position ajustée quand la sidebar est active */
transform: rotate(0deg);
}

/* Ajustement pour les appareils mobiles */
@media (max-width: 768px) {
.toggle-sidebar {
left: 0;
top: 1rem;
}

.sidebar.active + .main-content .toggle-sidebar {
left: 250px;
}
}

.toggle-sidebar,
.menu-btn {
padding: 0.5rem;
border: 1px solid var(--border-color);
border-radius: var(--radius-md);
background-color: transparent;
color: var(--text-color);
cursor: pointer;
display: flex;
align-items: center;
gap: 0.5rem;
transition: all 0.2s;
}

.toggle-sidebar:hover,
.menu-btn:hover {
background-color: var(--background-btn);
}

/* Chat Container Styles */
.chat-container {
flex: 1;
display: flex;
flex-direction: column;
overflow: hidden;
position: relative;
}

.chat-messages {
flex: 1;
overflow-y: auto;
padding: 1rem;
display: flex;
flex-direction: column;
gap: 1.5rem;
}

/* Message Styles */
.message {
display: flex;
gap: 1rem;
max-width: 80%;
animation: fadeIn 0.3s ease;
}

.message-avatar {
width: 60px;
height: 60px;
border-radius: 50%;
background: linear-gradient(
135deg,
var(--primary-color),
var(--secondary-color)
);
padding: 1px;
flex-shrink: 0;
}

.message-avatar img {
width: 100%;
height: 100%;
border-radius: 50%;
object-fit: cover;
background-color: var(--background-color);
padding: 1px;
}

.user-message {
margin-left: auto;
flex-direction: row-reverse;
}

.message-content {
background-color: var(--message-bg-bot);
padding: 1rem;
border-radius: var(--radius-lg);
box-shadow: var(--shadow-sm);
min-width: 200px;
}

.user-message .message-content {
background-color: var(--message-bg-user);
}

.message-header {
display: flex;
justify-content: space-between;
margin-bottom: 0.5rem;
font-size: 0.875rem;
color: var(--text-light);
}

/* Chat Input Styles */
.chat-input-container {
padding: 1rem;
background-color: var(--background-color);
border-top: 1px solid var(--border-color);
display: flex;
gap: 0.75rem;
position: relative;
}

.chat-input {
flex: 1;
padding: 0.75rem;
border: 1px solid var(--border-color);
border-radius: var(--radius-lg);
background-color: var(--message-bg-bot);
color: var(--text-color);
resize: none;
min-height: 44px;
max-height: 200px;
transition: all 0.2s;
}

.chat-input:focus {
outline: none;
border-color: var(--primary-color);
box-shadow: 0 0 0 2px rgba(124, 58, 237, 0.1);
}

.send-button {
padding: 0.75rem;
background-color: var(--primary-color);
color: white;
border: none;
border-radius: var(--radius-md);
cursor: pointer;
display: flex;
align-items: center;
justify-content: center;
transition: background-color 0.2s;
}

.send-button:hover {
background-color: var(--primary-hover);
}

/* Animations */
@keyframes fadeIn {
from {
opacity: 0;
transform: translateY(10px);
}
to {
opacity: 1;
transform: translateY(0);
}
}

/* Responsive Design */
@media (max-width: 1024px) {
.sidebar {
width: 240px;
}

.button-text {
font-size: 0.875rem;
}
}

@media (max-width: 768px) {
.sidebar {
position: fixed;
top: 0;
left: 0;
bottom: 0;
transform: translateX(-100%);
}

.sidebar.active {
transform: translateX(0);
}

.button-text {
display: none;
}

.message {
max-width: 90%;
}

.header-logo {
display: none;
}
}

@media (max-width: 480px) {
.header-right {
gap: 0.5rem;
}

.message {
max-width: 95%;
}

.message-avatar {
width: 32px;
height: 32px;
}

.chat-input-container {
padding: 0.75rem;
}
}

/* Styles du dropdown */
.dropdown {
position: relative;
}

.dropdown-menu {
position: absolute;
top: 100%;
right: 0;
margin-top: 0.5rem;
background-color: var(--dropdown-bg);
border: 1px solid var(--border-color);
border-radius: var(--radius-md);
box-shadow: var(--shadow-md);
min-width: 200px;
opacity: 0;
visibility: hidden;
transform: translateY(-10px);
transition: all 0.2s ease;
}

.dropdown.active .dropdown-menu {
opacity: 1;
visibility: visible;
transform: translateY(0);
}

.dropdown-item {
display: flex;
align-items: center;
gap: 0.75rem;
padding: 0.75rem 1rem;
color: var(--text-color);
text-decoration: none;
transition: background-color 0.2s;
}

.dropdown-item:hover {
background-color: var(--dropdown-hover);
}

.dropdown-item:first-child {
border-top-left-radius: var(--radius-md);
border-top-right-radius: var(--radius-md);
}

.dropdown-item:last-child {
border-bottom-left-radius: var(--radius-md);
border-bottom-right-radius: var(--radius-md);
}

/* Modification du bouton toggle-sidebar */
.toggle-sidebar {
transform: rotate(180deg); /* Pour que la flèche pointe vers la gauche */
}

.sidebar.active + .main-content .toggle-sidebar {
transform: rotate(0deg);
}

/* Styles existants conservés */

/* Styles des Modales */
.modal {
display: none;
position: fixed;
top: 0;
left: 0;
width: 100%;
height: 100%;
background-color: rgba(0, 0, 0, 0.5);
z-index: 1000;
opacity: 0;
transition: opacity 0.3s ease;
}

.modal.active {
display: flex;
opacity: 1;
align-items: center;
justify-content: center;
}

.modal-content {
background-color: var(--background-color);
border-radius: var(--radius-lg);
box-shadow: var(--shadow-md);
width: 90%;
max-width: 500px;
max-height: 90vh;
overflow-y: auto;
transform: translateY(-20px);
transition: transform 0.3s ease;
}

.modal.active .modal-content {
transform: translateY(0);
}

.modal-header {
padding: 1rem;
border-bottom: 1px solid var(--border-color);
display: flex;
justify-content: space-between;
align-items: center;
}

.modal-header h2 {
font-size: 1.25rem;
font-weight: 600;
color: var(--text-color);
}

.close-modal {
background: none;
border: none;
font-size: 1.5rem;
color: var(--text-light);
cursor: pointer;
padding: 0.5rem;
}

.modal-body {
padding: 1.5rem;
}

/* Styles pour la modale Compte */
.input-group {
margin-bottom: 1rem;
position: relative;
}

.input-group label {
display: block;
margin-bottom: 0.5rem;
color: var(--text-color);
font-size: 0.875rem;
}

.input-group input {
width: 100%;
padding: 0.75rem;
border: 1px solid var(--border-color);
border-radius: var(--radius-md);
background-color: var(--background-color);
color: var(--text-color);
}

.edit-button {
position: absolute;
right: 0.5rem;
top: 50%;
background: none;
border: none;
cursor: pointer;
color: var(--text-light);
padding: 0.25rem;
}

.save-button {
width: 100%;
padding: 0.75rem;
background-color: var(--primary-color);
color: white;
border: none;
border-radius: var(--radius-md);
cursor: pointer;
font-weight: 500;
margin-top: 1rem;
}

/* Styles pour la modale Thème */
.theme-options {
display: grid;
grid-template-columns: repeat(3, 1fr);
gap: 1rem;
}

.theme-option {
display: flex;
flex-direction: column;
align-items: center;
gap: 0.5rem;
padding: 1rem;
border: 1px solid var(--border-color);
border-radius: var(--radius-md);
background: none;
cursor: pointer;
color: var(--text-color);
}

.theme-option:hover {
background-color: var(--dropdown-hover);
}

.theme-option.active {
border-color: var(--primary-color);
background-color: var(--primary-color);
color: white;
}

/* Styles pour la modale Aide */
.help-chat {
display: flex;
flex-direction: column;
gap: 1rem;
}

.question-section h3 {
margin-bottom: 0.5rem;
color: var(--text-color);
}

.help-input {
display: flex;
gap: 0.5rem;
}

.help-input input {
flex: 1;
padding: 0.75rem;
border: 1px solid var(--border-color);
border-radius: var(--radius-md);
background-color: var(--background-color);
color: var(--text-color);
}

.send-help {
padding: 0.75rem;
background-color: var(--primary-color);
color: white;
border: none;
border-radius: var(--radius-md);
cursor: pointer;
}

/* Styles pour la modale de confirmation */
.confirmation-buttons {
display: flex;
gap: 1rem;
margin-top: 1.5rem;
}

.confirm-yes,
.confirm-no {
flex: 1;
padding: 0.75rem;
border: none;
border-radius: var(--radius-md);
cursor: pointer;
font-weight: 500;
}

.confirm-yes {
background-color: var(--primary-color);
color: white;
}

.confirm-no {
background-color: var(--border-color);
color: var(--text-color);
}

/* Styles pour la modale de sélection de projet */
.project-list {
display: flex;
flex-direction: column;
gap: 0.5rem;
}

.project-option {
padding: 0.75rem;
border: 1px solid var(--border-color);
border-radius: var(--radius-md);
background: none;
cursor: pointer;
color: var(--text-color);
text-align: left;
}

.project-option:hover {
background-color: var(--dropdown-hover);
}

/* Menu contextuel */
.context-menu {
display: none;
position: fixed;
background-color: var(--dropdown-bg);
border: 1px solid var(--border-color);
border-radius: var(--radius-md);
box-shadow: var(--shadow-md);
padding: 0.5rem;
z-index: 1000;
}

.context-menu.active {
display: block;
}

.context-menu-item {
display: flex;
align-items: center;
gap: 0.75rem;
padding: 0.75rem;
width: 100%;
border: none;
background: none;
color: var(--text-color);
cursor: pointer;
border-radius: var(--radius-sm);
}

.context-menu-item:hover {
background-color: var(--dropdown-hover);
}

/* Responsive Design pour les modales */
@media (max-width: 640px) {
.modal-content {
width: 95%;
margin: 1rem;
}

.theme-options {
grid-template-columns: 1fr;
}

.confirmation-buttons {
flex-direction: column;
}
}

/* Ajouter dans votre CSS */
.header-logo {
position: absolute;
left: 50%;
transform: translateX(-50%);
display: flex;
align-items: center;
z-index: 1; /* Assure que le logo reste au-dessus */
}

@media (max-width: 768px) {
.header-logo {
position: static;
transform: none;
margin: 0 auto;
}
}

/* Styles spécifiques pour les boutons de la modale de déconnexion */
.confirm-logout,
.cancel-logout {
flex: 1;
padding: 0.75rem;
border: none;
border-radius: var(--radius-md);
cursor: pointer;
font-weight: 500;
transition: all 0.2s ease;
}

.confirm-logout {
background-color: var(--border-color);
color: var(--text-color);
}

.confirm-logout:hover {
background-color: #901c1c;
color: white;
}

.confirm-logout:active {
transform: scale(0.98);
background-color: #dc2626;
color: white;
}

.cancel-logout {
background-color: var(--border-color);
color: var(--text-color);
}

.cancel-logout:hover {
background-color: #901c1c;
color: white;
}

.cancel-logout:active {
transform: scale(0.98);
background-color: #dc2626;
color: white;
}