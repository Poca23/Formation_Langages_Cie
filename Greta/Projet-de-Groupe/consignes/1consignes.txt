# Projet de groupe : MyChatGPT

acteurs : Ayman / Claire
LINKS : 
    Miro : https://miro.com/app/board/uXjVLoy3iZs=/
    figma : https://www.figma.com/design/hY9H6obaUWzpYBB2ais9jY/ClickTalk?node-id=0-1&p=f&t=g1kAdzqIh5HIwZkw-0
    mail : aemn22384@gmail.com / cndweb@gmail.com
    IDE ; VSCode (frontend) / IntelliJ (backend)

timeline : début mi-févirer à mi-mars
nom : ClickTalk
logo : Défini
technologies frontend : à définir
technologies backend : à définir
base de données : à définir

Concept :

Chatbot qui utilise l'apprentissage automatique (Machine Learning) pour répondre aux questions et aux interactions des utilisateurs. Le chatbot pourrait être conçu pour discuter sur des sujets spécifiques, tels que la technologie, la culture, la santé, etc.


Fonctionnalités :

Utilisateur :
    login/sign in ;
    créer une conversation ;
    créer une project ;
    créer une conversation dans le project;
    envoyer des messages ;
    supprimer une conversation ;
    consulter une liste de conversation ;
    log out/sign out ;
    delete all chats ;
    delete account ;

Projects :
    créer un project ;
    Modifier un projet ;
    Supprimer un projet ;
    créer une conversation dans le project ;
    modifier une conversation dan le projet ;
    Supprimer une conversation dans le projet ;
    ajouter des Instructions dans le project ;
    Modifier des instructions dans le projet

Conversations :
    Créer les conversation ;
    Envoyer des messages ;
    supprimer les conversations ;
    Formater la réponse qui est en md ;
    pouvoir copier le contenu de la réponse ;
    Réponse en temps réel (streaming de la réponse)
    ajouter message pu animation d'attente dans le front (3     points)
    conversation pour tic tac toc

Messages :
    Envoyer des messages ;
    Recevoir des réponses ;
    Stopper la réponse ;
    Date et heure du message ;
    copier le message de l'IA

Paramètres :
    bouton log out ;
    -> settings ;
    -> thème ;
    -> delete account ;
    -> Change password


Architecture :

Voici une proposition d'architecture pour votre projet :

Front-end :
Utilisez React pour créer l'interface utilisateur du chatbot.
Créez un formulaire de saisie pour que les utilisateurs puissent poser des questions ou écrire des messages.
Utilisez CSS pour personnaliser l'apparence du chatbot.
Back-end :
Utilisez Node.js pour créer un serveur qui gérera les requêtes et les réponses du chatbot.
Créez une API pour gérer les interactions entre le front-end et le back-end.
Utilisez une bibliothèque de machine learning telle que TensorFlow.js pour entraîner et déployer un modèle de langage qui permettra au chatbot de répondre aux questions et aux interactions des utilisateurs.
Base de données :
Utilisez MySQL ou Supabase pour stocker les données de conversation et les réponses du chatbot.
Créez des tables pour stocker les questions, les réponses, les utilisateurs et les métadonnées de conversation.
Fonctionnalités :

Timeline :

Étant donné que vous avez un mois pour compléter le projet, voici une proposition de planification :

Semaine 1 : Recherche et conception du projet
Semaine 2-3 : Développement du front-end et du back-end
Semaine 4 : Intégration du modèle de langage et test du chatbot

Gestion de projet :

répartition des tâches :

- Maquettage
- front statique
- front dynamique
- base de données
- back
- UX
- UI

CREATE DATABASE IF NOT EXISTS clicktalk_db;

use clicktalk_db;

CREATE TABLE IF NOT EXISTS users (
 id BIGINT AUTO_INCREMENT PRIMARY KEY ,
        email VARCHAR(255) NOT NULL UNIQUE,
        password VARCHAR(255) NOT NULL,
        role VARCHAR(50) NOT NULL,
        created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS conversations (
        id BIGINT AUTO_INCREMENT PRIMARY KEY,
        user_id bigint NOT NULL,
        title VARCHAR(255) NOT NULL,
        created_at TIMESTAMP DEFAULT NOW(),
        FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS projects(
        id BIGINT AUTO_INCREMENT PRIMARY KEY,
        title VARCHAR(255) NOT NULL ,
        context LONGTEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS project_conversation(
        project_id BIGINT,
        conv_id BIGINT UNIQUE,
        PRIMARY KEY (project_id,conv_id),
        FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE,
        FOREIGN KEY (conv_id) REFERENCES  conversations(id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS messages (
        id BIGINT AUTO_INCREMENT PRIMARY KEY,
        conv_id BIGINT NOT NULL,
        content LONGTEXT NOT NULL,
        is_bot BOOLEAN NOT NULL,
        created_at TIMESTAMP DEFAULT NOW(),
        FOREIGN KEY (conv_id) REFERENCES conversations(id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS settings (
        id BIGINT AUTO_INCREMENT PRIMARY KEY,
        user_id BIGINT NOT NULL,
        theme VARCHAR(255) NOT NULL,
        FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

Clés primaires :
id pour la table Users
id pour la table Conversations
id pour la table projects
(project_id, conv_id) pour la table project_conversation
id pour la table Messages
id pour la table Settings
Clés étrangères :
user_id dans la table Conversations référence la table Users
conv_id dans la table Messages référence la table Conversations
conv_id dans la table project_conversation référence la table conversation
project_id dans la table project_conversation référence la table projects
user_id dans la table Settings référence la table Users

Stack Complète :

Backend (Côté Serveur) :
Spring boot/java/jsoncrack/deepseek/MySQL
Design :
Figma, Miro, Responsive, Design System
Frontend (Vue Utilisateur) :
React(create-react-app), SASS, Context API, Jest, TypeScript.
Développement Outils :
GitHub, Postman, CI/CD/maven, Swagger
Securité :
Spring Security, HTTPS, validation, JWT
Déploiement :
Vercel
Versionning :
Git - GitFlow main/dev/feature/Hot-Fix